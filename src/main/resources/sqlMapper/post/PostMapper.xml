<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="PostMapper">

	<select id="findAllPost" parameterType="int" resultType="postvo">
		<![CDATA[
			select no, title, author, content, w_date, blog_id
			from post
			where blog_id = #{blog_id }
		]]>
	</select>
	
	<select id="findByNo" parameterType="Long" resultType="postvo">
		<![CDATA[
			select title, author, content, w_date, blog_id
			from post
			where no = #{no }
		]]>
	</select>
	
	<insert id="insert" parameterType="postvo">
		<![CDATA[
			insert
			into post
			values(null, #{title }, #{author }, #{content }, null, null, now(), #{blogId })
		]]>
	</insert>
	
	<update id="update" parameterType="postvo">
		<![CDATA[
			update post
			set title = #{title },
			 	content = #{content }
		 	where no = #{no }
		]]>
	</update>
	
	<delete id="delete" parameterType="Long">
		<![CDATA[
			delete
			from post
			where no = #{no }
		]]>
	</delete>

</mapper>